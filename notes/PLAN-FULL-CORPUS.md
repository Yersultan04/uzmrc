# UzMRC — План: полный корпус (PDF + HTML) + фронт

**Дата:** 2026-06-21
**Источник аудита:** `notes/AUDIT-2026-06-21.md`
**Цель:** взять ВСЁ содержимое uzmrc.uz (а не 50 PDF из 308), включая HTML-контент и языки; затем привести фронт к виду продукта для конечного пользователя.

## Принятые дефолты (по открытым решениям)
1. **Берём всё**, дедуп по содержимому (sha256). Старые годовые/квартальные серии оставляем (нужны для сравнения версий). Дубли ru/uz — обе версии (кросс-язык полезен для retrieval).
2. **Существенные факты (191)** собираем, помечаем `source`-категорией в метаданных. Решение «один RAG vs отдельная база для раскрытий» — после замера объёма (Фаза A6).
3. **Фронт** — дорабатываем текущий rag-cms + добавляем упрощённый клиентский режим (не переписываем с нуля).

---

## ФАЗА A — Данные (полный корпус)

### A1. Доработать скрапер: HTML-проход
- Добавить в `scraper.py` извлечение **текста HTML-страниц** (сейчас берёт только `/uploads/*.pdf`).
- Обходить контент-разделы: `refinance-programm/tahliliy-malumotlar`, `mortgage-market-of-uzbekistan/*` (bozor-va-tahlil, ipoteka-savodxonligi, ilmiy-va-ekspert-maqolalar, hayotiy-misollar), `shareholders-investors/*` (biznes-reja-va-strategiya, moliyaviy-hisobotlar, emissiyalar, muhim-faktlar, kpi, reytinglar), `press/*`, `about/*`.
- Языки: собирать uz + `ru/` + `en/` зеркала.
- Чистка HTML → текст (убрать nav/footer/скрипты), сохранять `corpus/html/<slug>.txt` + провенанс (url, раздел, язык, дата).

### A2. Перепроверить ссылки (свежий discovery)
- Перезапустить PDF-discovery (`scraper.py`) → свежий `all_file_urls.txt`, diff со старым (сайт мог обновиться с 10 июня).
- Свести карту HTML (firecrawl/собственный обход) — итоговый список всех URL.

### A3. Докачка
- `scraper.py --download` → все PDF в `corpus/all/` + текст `corpus/all_txt/` + `manifest.json` (sha256/bytes/pages/chars).
- HTML-текст в `corpus/html/`.

### A4. Качество и дедуп
- Дедуп по содержимому. Карантин: 404, пустой текст (сканы без OCR), мусор.
- Отчёт: «скачано N / ошибок M / пустых K / дублей D».
- При сканах без текста — пометить для OCR (опционально позже).

### A5. Пересборка RAG
- Создать RAG на полном наборе (embedding-кэш + Voyage voyage-3.5).
- Проверить кросс-язык ru↔uz↔en, прогнать `eval/run_eval.py` до/после (не просел ли retrieval).
- Обновить страницу «О системе» (новые цифры док/чанков).

### A6. Решение по архитектуре баз
- По факту объёмов решить: один RAG со `source`-метаданными vs отдельные базы (Нормативка / Аналитика-техника / Раскрытия-факты).

---

## ФАЗА B — Фронт (UX)

### B1. Фикс «мёртвых кнопок»
- `RagDetail.tsx:348-356`: при `status≠ready`/нет прав — показывать причину (тултип/баннер), а не молчаливый disabled.

### B2. Чат на главную
- Для не-admin дефолтный экран = чат с готовой базой. Убрать навигацию по «RAG'ам» из основного потока.

### B3. Скрыть dev-терминологию
- «чанки/эмбеддинги/индексация/status=ready» — только под ролью admin. Пользователь видит «Задать вопрос» и «Проверить документ».

### B4. Навигация и пустые состояния
- Подписи в сайдбаре, хлебные крошки, понятные empty-states с подсказками.

### B5. Проверка
- Playwright вживую + скриншоты до/после.

---

## Trello (тикеты)
Создаются в доске «Projects — Kanban 2026», проект UzMRC. Список карточек = пункты A1–A6, B1–B5.

## Next Action
A1 (доработка скрапера: HTML-проход) — в работе.
